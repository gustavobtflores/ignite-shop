import {createStitches} from '@stitches/react'

export const {
  config,
  styled,
  globalCss, 
  keyframes,
  theme,
  createTheme, 
  getCssText, 
  css
} = createStitches({
  theme: {
    colors: {
      rocketseat: "#8257e6"
    }
  }
})

